---
name: Bug report
about: Report a problem with Bluetooth-2-USB
title: ""
labels: bug
assignees: ""
---

## Target Host

<!-- What is the Pi connected to as a USB gadget? Include OS and host type. -->

## Smoketest Result

<!-- Say whether `bluetooth_2_usb smoketest --verbose` passed or failed. Do not paste its raw output. -->

## Debug Report

<!-- Attach the Markdown report generated by `bluetooth_2_usb debug --duration 10`. -->

To retrieve the latest redacted debug report from a Pi after running `debug`:

> [!NOTE]
> Change `PI_USER` and `PI_HOST` if your Pi uses a different SSH user or
> hostname. Keep the `.local` suffix when mDNS works; otherwise use the Pi's
> IP address or another resolvable hostname.

```bash
PI_USER=pi
PI_HOST=raspberrypi.local
PI_SSH="${PI_USER}@${PI_HOST}"

ssh "${PI_SSH}" 'sudo bluetooth_2_usb debug --duration 10'
DEBUG_REPORT=$(ssh "${PI_SSH}" 'ls -t /var/log/bluetooth_2_usb/debug_*.md | head -n 1')
scp "${PI_SSH}:${DEBUG_REPORT}" ./bluetooth_2_usb-debug-latest.md
```

## Reproduction Steps

<!-- List the steps that trigger the problem. -->

1.
2.
3.

## Recent Changes

<!-- For pairing, relay behavior, suspend/wake, service startup, or read-only mode, mention what changed recently. -->

## OTG Connection

<!-- Is the Pi connected to the target host through the OTG-capable data port? -->
